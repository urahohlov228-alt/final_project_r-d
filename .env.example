# --- LLM provider (Groq free tier by default; any OpenAI-compatible API works) ---
# Get a free key at https://console.groq.com/keys
LLM_API_KEY=gsk_your_groq_key_here
LLM_BASE_URL=https://api.groq.com/openai/v1
LLM_MODEL=llama-3.3-70b-versatile
ROUTER_MODEL=llama-3.1-8b-instant

# --- app ---
PORT=8080
ENV=dev
LOG_LEVEL=INFO

# --- security ---
# If set, POST /api/chat requires the "X-API-Key" header with this value.
APP_API_KEY=
RATE_LIMIT_PER_MINUTE=20
# Comma-separated CIDRs/IPs of upstream proxies whose X-Forwarded-For we
# trust for rate-limit keying. Empty = never trust XFF, use direct peer IP.
# For Cloud Run behind Google Load Balancer:
# TRUSTED_PROXIES=35.191.0.0/16,130.211.0.0/22
TRUSTED_PROXIES=

# --- advanced (defaults are fine) ---
# MCP_URL=http://127.0.0.1:8080/mcp
# MAX_AGENT_ITERATIONS=5
# RAG_TOP_K=4
